import * as React from 'react';
import {FC} from "react";

type Props = {};
export const CountryCardDetail: FC = (props: Props) => {
    return (
        <div>
            Country Detail
        </div>
    );
};
